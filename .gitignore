node_modules/
dist/
.DS_Store
*.local
.env
.env.*
!.env.example
.vite/
coverage/
.wrangler/

# python bytecode, from the recovery tooling in scripts/recover-from-video/
__pycache__/
*.pyc

# screenshots written by the scratch smokes (scratch/*.mjs); the scripts are
# committed, their output is evidence for one run and not a repo artefact.
scratch/*.png
